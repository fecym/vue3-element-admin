/**
 * 布局大小枚举
 */
const SizeEnum = {
  /**
   * 默认
   */
  DEFAULT: "default",

  /**
   * 大型
   */
  LARGE: "large",

  /**
   * 小型
   */
  SMALL: "small",
};

// 使其能在模块中使用，类似于 TypeScript 的 export
export { SizeEnum };
